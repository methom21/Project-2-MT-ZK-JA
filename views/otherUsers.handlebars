{{!-- lit of other users for loop i<2 shows 3 random users--}}

{{!-- view users roster button --}}

{{!-- image of strongest/newest character --}}

{{!-- show user name --}}


{{!-- 
{{#each users as |user| }}
<div class="row align-center mb-5">
  <div class="col-md-6">
    <h2>{{user.name}}</h2>
  </div>
  <div class="col-md-6">
    <h3><a href="mailto:{{user.email}}">{{user.email}}</a></h3>
  </div>
</div>
{{/each}} --}}