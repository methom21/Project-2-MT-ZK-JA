<div><h2>ROSTER TEST</h2></div>

  <script type="text/x-handlebars-template"></script>
  </script>
    {{#each users as |hero| }}
   <div class="col-md-12">
<div class="row align-center mb-5">
  <div class="col-md-3">
    <h2>{{hero.name}}</h2>
  </div>
  <div class="col-md-3 ">
    <h3><a href="mailto:{{hero.description}}">{{hero.power}}</a></h3>
  </div>
</div>
</div>
{{/each}}

 
   
      